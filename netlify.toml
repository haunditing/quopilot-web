[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://quopilot-api.up.railway.app"
  VITE_PREVIEW_ENABLED = "false"

# SPA: cualquier ruta cae a index.html (client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
